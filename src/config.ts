/**
 * Config de la SPA (solo front).
 * Sin backend propio ni variables de entorno: la app pega directo al BFF.
 * Si mañana hay otro ambiente, cambiá esta URL (o cargá un config.json estático).
 */
export const BFF_API_BASE_URL = 'https://tau-bff.telecom.com.ar'
